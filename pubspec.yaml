name: cryptopay_merchant
description: "CryptoPay Merchant Terminal"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.10.3

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  qr_flutter: ^4.1.0
  http: ^1.2.1
  audioplayers: ^6.0.0
  flutter_tts: ^3.8.5
  shared_preferences: ^2.2.2
  connectivity_plus: ^6.0.3
  network_info_plus: ^4.0.2
  local_auth: ^2.1.4
  mobile_scanner: ^5.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/sounds/payment_success.mp3
